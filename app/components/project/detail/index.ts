import { default as Classify } from "./classify-dialog"
import { default as Organization } from "./organization-dialog"
import { default as Panel } from "./panel"
import { default as Status } from "./status-card"

export const Detail = {
    Classify,
    Organization,
    Panel,
    Status,
}
