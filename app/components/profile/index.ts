import { default as ConnectedAccounts } from "./connected-accounts"
import { default as DangerZone } from "./danger-zone"
import { default as Fallback } from "./accounts-fallback"
import { default as Password } from "./password-card"

export const Account = {
    ConnectedAccounts,
    DangerZone,
    Fallback,
    Password,
}
