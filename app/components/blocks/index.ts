import { default as Alert } from "./alert"
import { default as ConfirmDelete } from "./confirm-delete"
import { default as Metadata } from "./metadata"
import { default as Sound } from "./sound-toggle"
import { default as Status } from "./status"
import { default as Terms } from "./terms-dialog"
import { default as Theme } from "./theme-toggle"

export const Blocks = {
    Alert,
    ConfirmDelete,
    Metadata,
    Sound,
    Status,
    Terms,
    Theme,
}
